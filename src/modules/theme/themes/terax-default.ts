import type { Theme } from "../types";

export const teraxDefault: Theme = {
  id: "terax-default",
  name: "Terax Default",
  description: "The default Gear theme",
  variants: {
    light: {},
    dark: {},
  },
};
